# Weather-Alert-Automation-using-n8n
This project automates weather alerts using n8n. It receives user details through an HTML form, fetches real-time weather information from a Weather API, evaluates conditions using IF logic, and automatically sends an email notification.
